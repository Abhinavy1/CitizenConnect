import colors from "./colors";

const theme = {
  colors,

  radius: {
    sm: "0.5rem",
    md: "0.75rem",
    lg: "1rem",
    xl: "1.5rem",
    full: "9999px",
  },

  shadow: {
    sm: "0 4px 12px rgba(0,0,0,.15)",
    md: "0 10px 30px rgba(0,0,0,.25)",
    lg: "0 20px 60px rgba(0,0,0,.35)",
  },

  transition: {
    fast: "200ms",
    normal: "300ms",
    slow: "500ms",
  },
};

export default theme;